# 简化版Dockerfile - 直接使用本地构建的dist目录
# 使用此Dockerfile前，需要在本地先构建前端：pnpm build:prod

FROM nginx:alpine

# 复制构建产物到nginx目录
COPY dist /usr/share/nginx/html

# 复制nginx配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 暴露端口
EXPOSE 80

# 启动nginx
CMD ["nginx", "-g", "daemon off;"]

