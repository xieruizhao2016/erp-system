# 生产管理菜单重组说明

## 📋 概述

为了更好的组织生产管理模块，在生产管理下添加了分类层级，将相关模块按照功能进行分类管理。

## 🎯 重组目标

在生产管理（ID: 5042）下添加分类菜单，将以下模块组织起来：
- 生产订单
- 设备管理
- BOM（产品BOM、BOM明细）
- MRP（MRP参数、MRP运算结果）
- 工单（工单、工单进度）
- 其他相关模块

## 📁 新的菜单结构

```
生产管理 (5042)
├── 基础数据 (6100)
│   ├── 产品BOM
│   ├── BOM明细
│   ├── 工艺路线
│   ├── 工艺路线明细
│   ├── 设备管理
│   └── 设备状态
│
├── 生产计划 (6101)
│   ├── 生产订单
│   ├── MRP参数
│   ├── MRP运算结果
│   ├── 生产排程
│   └── 排程明细
│
├── 生产执行 (6102)
│   ├── 工单
│   └── 工单进度
│
├── 质量管理 (6103)
│   ├── 质检标准
│   ├── 质检项目
│   ├── 质检记录
│   └── 质检明细
│
├── 成本管理 (6104)
│   ├── 标准成本
│   ├── 实际成本
│   └── 成本差异
│
└── 统计分析 (6105)
    ├── 工时统计
    ├── 生产KPI
    ├── 生产报表
    └── 看板配置
```

## 🔧 分类说明

### 1. 基础数据 (6100)
包含生产所需的基础数据配置：
- 产品BOM：产品物料清单
- BOM明细：BOM的详细组成
- 工艺路线：产品生产工艺流程
- 工艺路线明细：工艺路线的详细步骤
- 设备管理：生产设备台账
- 设备状态：设备运行状态监控

### 2. 生产计划 (6101)
包含生产计划相关的功能：
- 生产订单：生产订单管理
- MRP参数：物料需求计划参数配置
- MRP运算结果：MRP运算结果查看
- 生产排程：生产计划排程
- 排程明细：排程的详细安排

### 3. 生产执行 (6102)
包含生产执行过程的管理：
- 工单：生产工单管理
- 工单进度：工单执行进度跟踪

### 4. 质量管理 (6103)
包含质量相关的管理：
- 质检标准：质量检验标准
- 质检项目：质检项目配置
- 质检记录：质量检验记录
- 质检明细：质检详细记录

### 5. 成本管理 (6104)
包含成本相关的管理：
- 标准成本：产品标准成本
- 实际成本：实际生产成本
- 成本差异：成本差异分析

### 6. 统计分析 (6105)
包含生产数据统计分析：
- 工时统计：生产工时统计
- 生产KPI：生产关键指标
- 生产报表：生产报表查看
- 看板配置：生产看板配置

## 📝 执行步骤

1. **备份数据库**
   ```bash
   # 建议先备份 system_menu 表
   mysqldump -u username -p database_name system_menu > system_menu_backup.sql
   ```

2. **执行SQL脚本**
   ```bash
   mysql -u username -p database_name < sql/mysql/reorganize-production-menus-with-categories.sql
   ```

3. **验证菜单结构**
   - 登录系统，检查菜单结构是否正确
   - 确认所有模块都已正确分类
   - 测试各模块功能是否正常

## ⚠️ 注意事项

1. **菜单ID冲突**：如果系统中已存在ID为6100-6105的菜单，需要先调整脚本中的ID。

2. **权限配置**：重组后，原有的角色权限配置不会自动更新，需要：
   - 检查角色菜单权限配置
   - 如有必要，重新分配菜单权限

3. **菜单缓存**：执行脚本后，可能需要：
   - 清除系统缓存
   - 重新登录系统以刷新菜单

4. **模块缺失**：如果某些模块在系统中不存在，脚本会跳过这些模块，不会报错。

## 🔍 验证查询

执行以下SQL可以验证菜单重组是否成功：

```sql
-- 查看分类菜单
SELECT id, name, parent_id, sort, type 
FROM system_menu 
WHERE id IN (6100, 6101, 6102, 6103, 6104, 6105) 
AND deleted = 0
ORDER BY sort;

-- 查看各分类下的模块
SELECT 
    c.name AS category_name,
    m.name AS module_name,
    m.id AS module_id,
    m.sort
FROM system_menu c
LEFT JOIN system_menu m ON m.parent_id = c.id AND m.type = 2 AND m.deleted = 0
WHERE c.id IN (6100, 6101, 6102, 6103, 6104, 6105)
AND c.deleted = 0
ORDER BY c.sort, m.sort;
```

## 📅 更新记录

- 2025-01-27: 创建菜单重组脚本和说明文档

