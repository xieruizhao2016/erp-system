# 服务器软件安装清单

## 📋 项目技术栈

这是一个基于 **Spring Boot 2.7.18** + **Vue 3.2** 的 ERP 系统项目。

## 🔧 必需安装的软件

### 1. Java 开发环境

**版本要求：**
- JDK 8（推荐用于 Spring Boot 2.7）
- 或 JDK 17/21（如果使用 master-jdk17 分支）

**安装方式：**
- 通过宝塔面板：软件商店 → 运行环境 → Java 项目管理器
- 或手动安装：`sudo apt install openjdk-8-jdk` 或 `openjdk-17-jdk`

### 2. MySQL 数据库

**版本要求：**
- MySQL 5.7 或 8.0+

**安装方式：**
- 通过宝塔面板：软件商店 → 数据库 → MySQL（推荐）
- 或手动安装：`sudo apt install mysql-server`

**配置要求：**
- 创建数据库和用户
- 配置字符集为 utf8mb4
- 开放 3306 端口（如需要远程连接）

### 3. Redis 缓存

**版本要求：**
- Redis 5.0 / 6.0 / 7.0

**安装方式：**
- 通过宝塔面板：软件商店 → 数据库 → Redis（推荐）
- 或手动安装：`sudo apt install redis-server`

**配置要求：**
- 设置密码（生产环境必需）
- 开放 6379 端口（如需要远程连接）

### 4. Maven 构建工具

**版本要求：**
- Maven 3.6+

**安装方式：**
```bash
sudo apt install maven
# 或下载最新版本
wget https://dlcdn.apache.org/maven/maven-3/3.9.6/binaries/apache-maven-3.9.6-bin.tar.gz
tar -xzf apache-maven-3.9.6-bin.tar.gz
sudo mv apache-maven-3.9.6 /opt/maven
```

### 5. Node.js 和 npm（前端构建）

**版本要求：**
- Node.js 16+（推荐 18 LTS）
- npm 8+

**安装方式：**
- 通过宝塔面板：软件商店 → 运行环境 → Node.js 版本管理器（推荐）
- 或使用 nvm：
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 18
nvm use 18
```

### 6. Nginx Web 服务器（前端部署）

**版本要求：**
- Nginx 1.18+

**安装方式：**
- 通过宝塔面板：软件商店 → Web 服务器 → Nginx（推荐）
- 或手动安装：`sudo apt install nginx`

**用途：**
- 部署前端静态文件
- 反向代理后端 API

## 📦 已安装的软件

✅ **Docker** - 已安装（Docker 28.2.2）✅ 运行中
✅ **Docker Compose** - 已安装（1.29.2）
✅ **宝塔面板** - 已安装（7.x）✅ 运行中
✅ **MySQL** - 已安装（5.7.44）✅ 运行中
✅ **Redis** - 已安装（8.0.5）✅ 运行中
✅ **Nginx** - 已安装（1.28.0）✅ 运行中
✅ **Java (JDK)** - 已安装（OpenJDK 1.8.0_472）✅
✅ **Maven** - 已安装（3.8.7）✅
✅ **Node.js** - 已安装（18.20.8）✅
✅ **npm** - 已安装（10.8.2）✅

## ✅ 安装完成状态

**所有必需软件已全部安装完成！** 🎉

## 🚀 推荐安装顺序

### 方式一：通过宝塔面板安装（推荐）

1. **登录宝塔面板**
   - 地址：https://101.33.244.240:13794/5343ca33

2. **安装运行环境**
   - 软件商店 → 运行环境
   - 安装：**Java 项目管理器**（包含 JDK）
   - 安装：**Node.js 版本管理器**

3. **安装数据库**
   - 软件商店 → 数据库
   - 安装：**MySQL 8.0**（或 5.7）
   - 安装：**Redis 7.0**（或 6.0）

4. **安装 Web 服务器**
   - 软件商店 → Web 服务器
   - 安装：**Nginx**

5. **安装 Maven**
   - 通过 SSH 终端手动安装（见下方命令）

### 方式二：通过命令行安装

```bash
# 1. 安装 Java 8
sudo apt update
sudo apt install -y openjdk-8-jdk

# 2. 安装 MySQL
sudo apt install -y mysql-server

# 3. 安装 Redis
sudo apt install -y redis-server

# 4. 安装 Maven
sudo apt install -y maven

# 5. 安装 Node.js（使用 nvm）
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc
nvm install 18
nvm use 18

# 6. 安装 Nginx
sudo apt install -y nginx
```

## ✅ 安装后验证

```bash
# 检查 Java
java -version
javac -version

# 检查 MySQL
mysql --version
sudo systemctl status mysql

# 检查 Redis
redis-cli --version
redis-cli ping

# 检查 Maven
mvn -version

# 检查 Node.js
node -v
npm -v

# 检查 Nginx
nginx -v
sudo systemctl status nginx
```

## 🔐 安全配置建议

### MySQL
- 运行 `sudo mysql_secure_installation` 进行安全配置
- 创建专用数据库用户（不要使用 root）
- 设置强密码
- 限制远程访问（如不需要）

### Redis
- 设置密码：编辑 `/etc/redis/redis.conf`，设置 `requirepass`
- 绑定本地：设置 `bind 127.0.0.1`
- 禁用危险命令

### Nginx
- 配置 SSL 证书（HTTPS）
- 设置防火墙规则
- 限制访问频率

## 📊 当前安装状态检查（2025-12-06）

### ✅ 已安装并运行
- Docker 28.2.2 ✅
- Docker Compose 1.29.2 ✅
- 宝塔面板 7.x ✅
- MySQL 5.7.44 ✅

### ✅ 已安装并运行
- Redis 8.0.5 ✅
- Nginx 1.28.0 ✅
- Java (OpenJDK 1.8.0_472) ✅
- Maven 3.8.7 ✅
- Node.js 18.20.8 ✅
- npm 10.8.2 ✅

## ✅ 安装完成

所有必需软件已成功安装！服务器已准备好运行 ERP 系统项目。

## 📝 下一步

安装完所有软件后，请参考：
- [部署文档](./部署指南.md)（待创建）
- [数据库初始化脚本](../sql/)
- [配置文件说明](../config/)

---

**提示：** 建议优先使用宝塔面板安装，操作简单且便于管理。

