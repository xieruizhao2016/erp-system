# 快速部署指南

## 🚀 一键部署（推荐）

### 在服务器上执行以下命令：

```bash
# 1. 上传项目到服务器后，进入项目目录
cd /opt/erp-system

# 2. 运行快速环境准备脚本（首次部署需要）
chmod +x deploy/scripts/quick-deploy.sh
./deploy/scripts/quick-deploy.sh

# 3. 配置环境变量
cp deploy/config/env.prod.example script/docker/.env
vi script/docker/.env  # 修改密码等配置

# 4. 运行部署脚本
chmod +x deploy/scripts/deploy-volcano.sh
./deploy/scripts/deploy-volcano.sh
```

## 📝 详细步骤

### 步骤1: 准备服务器环境

```bash
cd /opt/erp-system
./deploy/scripts/quick-deploy.sh
```

此脚本会自动安装：
- Docker
- Docker Compose
- 配置防火墙规则

### 步骤2: 配置环境变量

```bash
# 复制配置示例文件
cp deploy/config/env.prod.example script/docker/.env

# 编辑配置文件（必须修改密码）
vi script/docker/.env
```

**必须修改的配置项：**
- `MYSQL_ROOT_PASSWORD`: 数据库root密码
- `MASTER_DATASOURCE_PASSWORD`: 数据库连接密码
- `SLAVE_DATASOURCE_PASSWORD`: 从库连接密码
- `REDIS_PASSWORD`: Redis密码（建议设置）

### 步骤3: 构建后端（如需要）

如果后端JAR文件不存在，部署脚本会自动构建。也可以手动构建：

```bash
cd /opt/erp-system
mvn clean package -DskipTests
```

### 步骤4: 部署服务

```bash
cd /opt/erp-system
./deploy/scripts/deploy-volcano.sh
```

### 步骤5: 验证部署

```bash
# 查看服务状态
cd /opt/erp-system/script/docker
docker-compose -f docker-compose.prod.yml ps

# 查看日志
docker-compose -f docker-compose.prod.yml logs -f

# 访问前端
# http://你的服务器IP

# 访问后端API
# http://你的服务器IP:48080
```

## 🔧 常用命令

### 查看服务状态
```bash
cd /opt/erp-system/script/docker
docker-compose -f docker-compose.prod.yml ps
```

### 查看日志
```bash
# 所有服务
docker-compose -f docker-compose.prod.yml logs -f

# 特定服务
docker-compose -f docker-compose.prod.yml logs -f server
docker-compose -f docker-compose.prod.yml logs -f admin
```

### 重启服务
```bash
docker-compose -f docker-compose.prod.yml restart
```

### 停止服务
```bash
docker-compose -f docker-compose.prod.yml down
```

### 更新服务
```bash
# 拉取最新代码后
docker-compose -f docker-compose.prod.yml up -d --build
```

## ⚠️ 重要提示

1. **首次部署必须修改密码**
   - 编辑 `script/docker/.env` 文件
   - 修改所有包含"请修改为强密码"的配置项

2. **默认登录账号**
   - 用户名：admin
   - 密码：admin123
   - ⚠️ 首次登录后请立即修改密码！

3. **端口要求**
   - 确保防火墙开放 80 和 48080 端口

4. **服务器要求**
   - 最低配置：2核4G
   - 推荐配置：4核8G

## 📚 更多文档

- **完整部署指南**: [docs/火山引擎部署指南.md](./docs/火山引擎部署指南.md)
- **快速开始**: [docs/火山引擎快速开始.md](./docs/火山引擎快速开始.md)
- **检查清单**: [docs/部署检查清单.md](./docs/部署检查清单.md)

## 🆘 遇到问题？

1. 查看完整部署指南中的"常见问题"部分
2. 查看部署检查清单
3. 检查服务日志：`docker-compose logs`
4. 提交Issue获取帮助

---

**祝部署顺利！** 🎉

