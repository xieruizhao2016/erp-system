# 🚀 ERP生产管理模块快速启动指南

## 📋 前置条件检查

### 1. 数据库服务
确保 MySQL 和 Redis 正在运行：
```bash
# 检查MySQL
mysql -u root -p -e "SELECT 1;" ruoyi-vue-pro

# 检查Redis
redis-cli ping
```

如果未运行，启动Docker服务：
```bash
cd script/docker
docker-compose up -d mysql redis
```

### 2. Java环境
确保Java已安装：
```bash
java -version
```

如果未安装，使用Homebrew安装：
```bash
brew install openjdk@8
```

## 📝 步骤1：导入菜单SQL

### 方式一：使用脚本（推荐）
```bash
./scripts/import-menu-sql.sh ruoyi-vue-pro root
```

### 方式二：直接执行SQL
```bash
mysql -u root -p ruoyi-vue-pro < sql/mysql/erp_production_menus.sql
```

### 验证导入
```bash
mysql -u root -p ruoyi-vue-pro -e "SELECT COUNT(*) as menu_count FROM system_menu WHERE parent_id = 5042;"
```
应该返回 24（24个模块菜单）

## 🔧 步骤2：编译后端代码（如果需要）

如果代码有修改，需要重新编译：
```bash
cd yudao-module-erp
mvn clean compile -DskipTests
```

如果编译成功，打包jar：
```bash
cd ..
mvn clean package -DskipTests
```

## 🚀 步骤3：启动后端服务

### 方式一：使用启动脚本（推荐）
```bash
# 前台启动（可以看到日志）
./start-backend.sh

# 后台启动
./start-backend.sh -d
```

### 方式二：使用Maven
```bash
cd yudao-server
mvn spring-boot:run
```

### 验证后端启动
等待启动完成后，访问：
- API文档：http://localhost:48080/swagger-ui
- 健康检查：http://localhost:48080/admin-api/system/auth/get-permission-info

## 🎨 步骤4：启动前端服务

```bash
cd original-yudao-ui
npm install  # 如果依赖未安装
npm run dev  # 或 pnpm dev
```

前端服务通常在 `http://localhost:5173` 启动

## ✅ 步骤5：测试功能

### 1. 登录系统
- 访问前端地址：http://localhost:5173
- 默认账号：admin / admin123

### 2. 检查菜单
登录后，在左侧菜单中应该能看到：
- **ERP系统** → **生产管理**
  - 生产订单
  - 产品BOM
  - 工艺路线
  - ...（共24个模块）

### 3. 测试功能模块
选择一个模块（如"生产订单"）：
1. 点击菜单进入列表页
2. 点击"新增"按钮创建数据
3. 测试查询、编辑、删除等功能
4. 检查权限控制是否正常

### 4. 配置角色权限
1. 进入：**系统管理** → **角色管理**
2. 编辑角色，分配新模块的权限
3. 测试不同角色的权限控制

## 🔍 常见问题排查

### 问题1：菜单未显示
**原因**：菜单SQL未导入或导入失败

**解决**：
```bash
# 检查菜单是否存在
mysql -u root -p ruoyi-vue-pro -e "SELECT id, name, path FROM system_menu WHERE parent_id = 5042;"

# 如果不存在，重新导入
mysql -u root -p ruoyi-vue-pro < sql/mysql/erp_production_menus.sql
```

### 问题2：前端页面404
**原因**：路由路径不匹配

**解决**：
1. 检查菜单SQL中的 `component` 路径
2. 检查前端文件是否存在：`original-yudao-ui/src/views/erp/`
3. 确保路径一致（如：`erp/productionorder/index`）

### 问题3：后端API 404
**原因**：Controller路径不匹配

**解决**：
1. 检查Controller的 `@RequestMapping` 路径
2. 检查Swagger文档：http://localhost:48080/swagger-ui
3. 确认API路径格式：`/erp/{module-name}/...`

### 问题4：权限不足
**原因**：角色未分配权限

**解决**：
1. 进入：**系统管理** → **角色管理**
2. 编辑角色，勾选新模块的权限
3. 重新登录测试

### 问题5：编译错误
**原因**：代码有语法错误或缺失依赖

**解决**：
```bash
# 重新编译
cd yudao-module-erp
mvn clean compile -DskipTests

# 查看详细错误
mvn clean compile -DskipTests -X
```

## 📊 验证清单

- [ ] 菜单SQL已导入
- [ ] 后端服务已启动（端口48080）
- [ ] 前端服务已启动（端口5173）
- [ ] 可以登录系统
- [ ] 可以看到"生产管理"菜单
- [ ] 可以访问各个模块页面
- [ ] 可以创建、查询、编辑、删除数据
- [ ] 权限控制正常工作

## 🎉 完成！

如果所有检查项都通过，说明系统已成功部署并可以正常使用！

## 📞 需要帮助？

如果遇到问题，请检查：
1. 日志文件：`nohup.out`（后端日志）
2. 浏览器控制台（前端错误）
3. 数据库连接是否正常
4. Redis连接是否正常

